{
  "name": "practice",
  "version": "1.0.0",
  "description": "Practice for nodejs backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "deploy:ui": "rm rf dist && cd ../notesFrontend && npm run build && cp -r dist ../notesFullstack",
    "deploy": "git add . && git commit -m 'complete build' && git push",
    "deploy:full": "npm run deploy:ui && git add . && git commit -m uibuild && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node",
    "backend",
    "notes"
  ],
  "author": "Nivedita/Ivin",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "mongoose": "^8.7.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
